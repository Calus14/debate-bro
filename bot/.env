DISCORD_TOKEN=${BOT_API_TOKEN}

# Recording behavior
END_SILENCE_MS=30000            # user considered silent after 30s
FLUSH_INTERVAL_MINUTES=5        # split & upload every 5 minutes
# FLUSH_INTERVAL_MS=300000      # (optional) overrides minutes if set

# S3 (shared)
AWS_REGION=us-east-2
S3_BUCKET_NAME=debate-bro-recordings-dev

# Do NOT put AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY here for prod.
# On EC2, the aws-sdk will use the instance role automatically.